#### Мои компоненты React.js

Кнопки:
```
# Отправка формы
ButtonSubmit 
    props.id
    props.caption
    props.color

# Простая кнопка принимает функцию onClick
ButtonSimple
    props.id
    props.caption
    props.color
    props.onClick
```
